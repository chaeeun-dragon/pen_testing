// HAEON-DIAG-01 실행 증거를 읽어 시연 촬영 큐시트를 생성한다.
import fs from 'node:fs';
import path from 'node:path';

const RUNS = process.env.RUNS_DIR || 'evidence/runs';
const OUT = process.env.OUT_FILE || 'docs/scenarios/haeon-lab-demo-shooting-guide-v1.0.html';
const readJson = file => { try { return JSON.parse(fs.readFileSync(file, 'utf8')); } catch { return null; } };
const readText = file => { try { return fs.readFileSync(file, 'utf8'); } catch { return ''; } };
const esc = value => String(value ?? '').replace(/[&<>"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const dirs = prefix => {
  try { return fs.readdirSync(RUNS, {withFileTypes:true}).filter(entry => entry.isDirectory() && entry.name.startsWith(prefix)).map(entry => path.join(RUNS, entry.name)).sort().reverse(); }
  catch { return []; }
};
const latest = prefix => dirs(prefix)[0] || null;
const diagRuns = dirs('HAEON-DIAG-').filter(dir => /^HAEON-DIAG-\d{14}-\d{4}$/.test(path.basename(dir)));
const beforeDir = diagRuns.find(dir => readJson(path.join(dir, 'scenario.json'))?.mode === 'before') || null;
const afterDir = diagRuns.find(dir => readJson(path.join(dir, 'scenario.json'))?.mode === 'after') || null;
const normalDir = latest('NORMAL-');
const normal = normalDir && readJson(path.join(normalDir, 'response.json'));
const before = beforeDir ? {
  scenario: readJson(path.join(beforeDir, 'scenario.json')),
  attack: readJson(path.join(beforeDir, 'attack-response.json')),
  records: readJson(path.join(beforeDir, 'records.json')),
  transfer: readJson(path.join(beforeDir, 'exfiltration.json')),
  response: readJson(path.join(beforeDir, 'response-action.json')),
  state: readJson(path.join(beforeDir, 'run-state.json')),
  events: readJson(path.join(beforeDir, 'events.json')),
} : null;
const after = afterDir ? {
  scenario: readJson(path.join(afterDir, 'scenario.json')),
  attack: readJson(path.join(afterDir, 'attack-response.json')),
  state: readJson(path.join(afterDir, 'run-state.json')),
  summary: readText(path.join(afterDir, 'summary.txt')),
} : null;
const recordsCount = before?.records?.recordCount ?? before?.records?.records?.length ?? '-';
const events = Array.isArray(before?.events) ? before.events : [];
const generated = new Date().toLocaleString('ko-KR', {timeZone:'Asia/Seoul', hour12:false});
const runName = dir => dir ? path.basename(dir) : '실행 기록 없음';
const status = (value, fallback='기록 없음') => esc(value || fallback);
const term = (title, command, output, tone='') => `<article class="terminal ${tone}"><div class="termbar"><i></i><i></i><i></i><span>${esc(title)}</span></div><pre><b>$ ${esc(command)}</b>\n${esc(output)}</pre></article>`;
const eventRows = events.length ? events.map(event => `<tr><td>${esc(event.eventType || '-')}</td><td>${esc(event.result || '-')}</td><td>${esc(event.occurredAt || '-')}</td></tr>`).join('') : '<tr><td colspan="3">이벤트 증거가 아직 없습니다. RUN_ID를 지정해 verify를 실행하세요.</td></tr>';

const html = `<!doctype html>
<html lang="ko"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>해온카드 시연 촬영 큐시트 · HAEON-DIAG-01</title>
<style>
:root{--ink:#202827;--muted:#687571;--line:#dfe8e4;--paper:#fff;--bg:#f4f7f5;--brand:#087c72;--deep:#073d39;--mint:#e8f5f0;--red:#b33e49;--redbg:#fff0f1;--blue:#176c94;--bluebg:#edf7fb;--amber:#94631a;--terminal:#111a18}
*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font-family:"Pretendard Variable",Pretendard,"Noto Sans KR",system-ui,sans-serif;line-height:1.62;word-break:keep-all}.hero{background:linear-gradient(120deg,#092f2d,#0c746d 68%,#4e9a81);color:#fff;padding:42px 22px 36px}.inner,.wrap{max-width:1120px;margin:auto}.eyebrow{font-size:11px;letter-spacing:.14em;font-weight:850;color:#bfe9dc}.hero h1{font-size:clamp(30px,5vw,48px);line-height:1.16;letter-spacing:-.04em;margin:10px 0}.hero p{margin:0;max-width:800px;color:#e1f2ec;font-size:14px}.meta{display:flex;gap:8px;flex-wrap:wrap;margin-top:18px}.pill{border:1px solid #ffffff42;background:#ffffff12;border-radius:99px;padding:6px 10px;font-size:11px}.nav{position:sticky;top:0;background:#ffffffed;backdrop-filter:blur(10px);border-bottom:1px solid var(--line);z-index:3}.nav .inner{display:flex;gap:6px;overflow:auto;padding:9px 14px}.nav a{white-space:nowrap;text-decoration:none;color:#53655f;padding:6px 9px;border-radius:6px;font-size:11px;font-weight:750}.nav a:hover{background:var(--mint);color:var(--brand)}main{padding:22px 20px 54px}.intro{display:grid;grid-template-columns:1.2fr .8fr;gap:12px}.card,.step{background:var(--paper);border:1px solid var(--line);border-radius:12px;box-shadow:0 7px 22px #143a3209}.card{padding:17px}.card h2,.card h3{margin:0 0 7px}.card h2{font-size:16px}.card h3{font-size:13px}.card p{margin:5px 0;color:var(--muted);font-size:12px}.flow{display:flex;align-items:center;gap:7px;flex-wrap:wrap;margin-top:12px}.flow span{background:var(--mint);border-radius:6px;padding:6px 8px;color:#185a4d;font-size:10px;font-weight:800}.flow b{color:#9aa7a3}.list{padding-left:17px;margin:5px 0;color:var(--muted);font-size:11px}.list li{padding:3px 0}.step{scroll-margin-top:55px;margin-top:13px;overflow:hidden}.stephead{display:flex;align-items:flex-start;gap:10px;padding:13px 16px;border-bottom:1px solid var(--line)}.stephead.red{background:linear-gradient(90deg,var(--redbg),#fff)}.stephead.blue{background:linear-gradient(90deg,var(--bluebg),#fff)}.stephead.green{background:linear-gradient(90deg,var(--mint),#fff)}.num{width:29px;height:29px;display:grid;place-items:center;flex:0 0 auto;border-radius:8px;background:var(--brand);color:white;font-weight:900}.red .num{background:var(--red)}.blue .num{background:var(--blue)}.stephead h2{margin:0;font-size:15px}.stephead p{margin:2px 0 0;color:var(--muted);font-size:11px}.tag{margin-left:auto;border-radius:99px;padding:4px 8px;background:white;border:1px solid var(--line);font-size:10px;font-weight:800;white-space:nowrap}.body{padding:15px 16px}.cols{display:grid;grid-template-columns:1fr 1fr;gap:12px}.terminal{margin:9px 0 0;border-radius:9px;background:var(--terminal);color:#d8eee6;overflow:hidden}.terminal.red{box-shadow:inset 4px 0 var(--red)}.terminal.blue{box-shadow:inset 4px 0 var(--blue)}.terminal.green{box-shadow:inset 4px 0 #21997d}.termbar{height:29px;display:flex;align-items:center;gap:5px;background:#1d2927;padding:0 11px}.termbar i{width:7px;height:7px;border-radius:50%;background:#ff7066}.termbar i:nth-child(2){background:#efbd4b}.termbar i:nth-child(3){background:#4ec77c}.termbar span{margin-left:4px;color:#9eb5ae;font:10px ui-monospace,monospace}.terminal pre{margin:0;padding:12px;white-space:pre-wrap;overflow-wrap:anywhere;font:11px/1.6 ui-monospace,Consolas,monospace}.terminal b{color:#fff}.say,.note{margin-top:9px;padding:10px 12px;border-radius:7px;font-size:11px}.say{border-left:3px solid #dba839;background:#fff8e8;color:#665125}.note{background:#f5f8f7;border:1px solid var(--line);color:var(--muted)}.facts{display:grid;grid-template-columns:repeat(4,1fr);gap:7px;margin:9px 0}.fact{border:1px solid var(--line);border-radius:7px;padding:8px 9px;background:#fafcfb}.fact small{display:block;color:#83908b;font-size:9px}.fact strong{display:block;margin-top:2px;font-size:13px;overflow-wrap:anywhere}.ok{color:#08765e}.bad{color:var(--red)}table{width:100%;border-collapse:collapse;font-size:10px;margin-top:8px}th,td{padding:7px;border-bottom:1px solid var(--line);text-align:left;overflow-wrap:anywhere}th{background:#f3f8f6;color:#45625a}.foot{margin-top:16px;padding:14px;color:var(--muted);font-size:10px;border-top:1px solid var(--line)}
@media(max-width:740px){.intro,.cols{grid-template-columns:1fr}.facts{grid-template-columns:1fr 1fr}.tag{margin-left:39px}.hero{padding:30px 16px}.wrap{padding-left:14px;padding-right:14px}}@media print{.nav{display:none}.step,.card{break-inside:avoid;box-shadow:none}.hero{padding:24px}.wrap{padding-left:0;padding-right:0}}
</style></head><body>
<header class="hero"><div class="inner"><div class="eyebrow">HAEON CARD · MERCHANT SUPPORT</div><h1>결제 연동 진단 API<br>시연 촬영 큐시트</h1><p>가맹점 정상 진단부터 침입 탐지와 대응, 회원 보호 안내까지 HAEON-DIAG-01의 흐름을 촬영합니다.</p><div class="meta"><span class="pill">실행 ${esc(runName(beforeDir))}</span><span class="pill">가맹점 포털 → 진단 API</span><span class="pill">정상 진단 → Before → 대응 → After</span><span class="pill">생성 ${esc(generated)}</span></div></div></header>
<nav class="nav"><div class="inner"><a href="#prep">준비</a><a href="#baseline">정상 진단</a><a href="#before">최초 진입</a><a href="#evidence">증거·탐지</a><a href="#respond">대응</a><a href="#after">통제 확인</a><a href="#member">회원 보호</a></div></nav>
<main class="wrap">
<section class="intro" id="prep"><article class="card"><h2>촬영 순서</h2><div class="flow"><span>가맹점 로그인</span><b>›</b><span>정상 연결 확인</span><b>›</b><span>진단 API 침입</span><b>›</b><span>자료·이벤트 확인</span><b>›</b><span>대응·통제 확인</span></div><p>가맹점 포털의 결제 연동 진단 기능이 최초 진입점입니다. 브라우저에서는 토큰이나 비밀번호를 노출하지 않습니다.</p></article><article class="card"><h3>녹화 전 확인</h3><ul class="list"><li>가맹점 포털: <code>http://haeon.localhost:8090/merchant/</code></li><li>회원 마이페이지: <code>http://127.0.0.1:8085/mypage</code></li><li>운영자 제어 API는 로컬 명령으로만 사용합니다.</li></ul></article></section>
<section class="step" id="baseline"><div class="stephead green"><div class="num">1</div><div><h2>가맹점 로그인과 정상 진단</h2><p>등록된 결제 연동 대상을 선택해 정상 응답을 확인합니다.</p></div><span class="tag">기준선</span></div><div class="body cols"><div>${term('정상 진단','브라우저에서 가맹점 로그인 → 결제 연동 진단 → 연결 확인',`대상: diag-target-mart\n결과: ${status(normal?.result)}\n응답 시간: ${normal?.latencyMs ?? '-'} ms`,'green')}</div><div>${term('실행 증거','bash tools/haeon-diagnostic-lab.sh normal',normal ? `실행: ${runName(normalDir)}\nresult=${normal.result || '-'}` : '정상 진단 증거가 없습니다.','green')}<div class="say">“등록 대상 연결 확인이 정상 처리되는 것을 기준선으로 남기고, 같은 진단 기능에서 비정상 입력이 어떻게 처리되는지 살펴보겠습니다.”</div></div></div></section>
<section class="step" id="before"><div class="stephead red"><div class="num">2</div><div><h2>최초 진입 · 제한 세션 생성</h2><p>가맹점 진단 API의 취약 입력 처리 경로에서 세션이 만들어집니다.</p></div><span class="tag">공격자 시점</span></div><div class="body cols"><div>${term('실행','bash tools/haeon-diagnostic-lab.sh before',before ? `실행 ID: ${runName(beforeDir)}\n모드: ${before.scenario?.mode || '-'}\n진단 응답: ${before.attack?.result || before.attack?.errorCode || '-'}` : 'Before 실행 증거가 없습니다.','red')}</div><div><div class="facts"><div class="fact"><small>진단 요청</small><strong>${status(before?.attack?.result)}</strong></div><div class="fact"><small>세션</small><strong>${before?.attack?.shellSessionToken ? '생성됨' : '증거 없음'}</strong></div><div class="fact"><small>자료 조회</small><strong>${esc(recordsCount)}건</strong></div><div class="fact"><small>전송 상태</small><strong>${status(before?.transfer?.receiverStatus)}</strong></div></div><div class="say">“최초 접근은 가맹점의 결제 연동 진단 API입니다. 제한 세션에서 조회와 지정된 수신 전송이 이어집니다.”</div><div class="note">세션 토큰은 응답 증거에 포함될 수 있으므로 화면에 직접 열지 않습니다.</div></div></div></section>
<section class="step" id="evidence"><div class="stephead blue"><div class="num">3</div><div><h2>자료 전송과 이벤트 기반 탐지</h2><p>조회 건수와 수신 증거, 실행별 이벤트를 대조합니다.</p></div><span class="tag">방어자 시점</span></div><div class="body"><div class="facts"><div class="fact"><small>조회 건수</small><strong>${esc(recordsCount)}건</strong></div><div class="fact"><small>수신 결과</small><strong>${status(before?.transfer?.receiverStatus)}</strong></div><div class="fact"><small>SHA-256</small><strong>${status(before?.transfer?.payloadSha256, '미기록').slice(0,16)}${before?.transfer?.payloadSha256 ? '…' : ''}</strong></div><div class="fact"><small>탐지 상태</small><strong>${status(before?.state?.alertStatus || before?.attack?.result)}</strong></div></div><table><thead><tr><th>이벤트</th><th>결과</th><th>발생 시각</th></tr></thead><tbody>${eventRows}</tbody></table><div class="say">“세션 생성, 자료 접근, 전송 이벤트를 실행 ID로 묶어 확인합니다. 경보는 관측된 이벤트 결과에서 확인합니다.”</div></div></section>
<section class="step" id="respond"><div class="stephead blue"><div class="num">4</div><div><h2>운영자 대응과 세션 폐기</h2><p>경보를 확인한 뒤 실행 단위로 접근을 차단합니다.</p></div><span class="tag">대응</span></div><div class="body cols"><div>${term('대응 및 상태 확인',`RUN_ID=${runName(beforeDir)} bash tools/haeon-diagnostic-lab.sh respond\nRUN_ID=${runName(beforeDir)} bash tools/haeon-diagnostic-lab.sh verify`,before ? `응답: ${before.response?.result || '-'}\n실행 상태: ${before.state?.status || '-'}\n경보 상태: ${before.state?.alertStatus || '-'}` : 'Before 실행을 먼저 완료하세요.','blue')}</div><div><div class="facts"><div class="fact"><small>대응 결과</small><strong class="ok">${status(before?.response?.result)}</strong></div><div class="fact"><small>실행 상태</small><strong class="ok">${status(before?.state?.status)}</strong></div><div class="fact"><small>경보 상태</small><strong class="ok">${status(before?.state?.alertStatus)}</strong></div><div class="fact"><small>보호 안내</small><strong>${before?.response?.protectionNoticeCreated ? '기록됨' : '확인 필요'}</strong></div></div><div class="say">“대응 이후 해당 실행의 세션을 사용할 수 없고, 영향 회원에게 추가 확인 안내가 연결됩니다.”</div></div></div></section>
<section class="step" id="after"><div class="stephead green"><div class="num">5</div><div><h2>개선 통제 · 동일 입력 차단</h2><p>같은 의미의 비정상 진단 입력은 거부되고 정상 진단은 계속됩니다.</p></div><span class="tag">통제 확인</span></div><div class="body cols"><div>${term('실행','bash tools/haeon-diagnostic-lab.sh after',after ? `실행 ID: ${runName(afterDir)}\nHTTP: ${after.attack?.httpStatus ?? after.attack?.status ?? after.summary.match(/attackHttp=(\\d+)/)?.[1] ?? '-'}\n오류 코드: ${after.attack?.errorCode || '-'}` : 'After 실행 증거가 없습니다.','green')}</div><div><div class="facts"><div class="fact"><small>요청 처리</small><strong class="ok">${status(after?.attack?.errorCode, after?.attack?.result || '기록 없음')}</strong></div><div class="fact"><small>세션 생성</small><strong>${after?.attack?.shellSessionToken ? '확인 필요' : '없음'}</strong></div><div class="fact"><small>정상 진단</small><strong>${status(normal?.result)}</strong></div><div class="fact"><small>실행 상태</small><strong>${status(after?.state?.status)}</strong></div></div><div class="say">“진단 API는 등록 대상의 정상 연결 확인을 유지하면서 허용되지 않은 옵션을 차단합니다.”</div></div></div></section>
<section class="step" id="member"><div class="stephead green"><div class="num">6</div><div><h2>회원 보호 안내</h2><p>마이페이지에서 영향 회원 안내와 확인 완료 상태를 확인합니다.</p></div><span class="tag">회원 화면</span></div><div class="body"><div class="cols"><div class="card"><h3>영향 회원</h3><p>haeon01로 로그인해 추가 확인 안내, 발생 시각, 확인 버튼을 확인합니다. 확인 후 완료 상태가 유지되는지 확인합니다.</p></div><div class="card"><h3>비교 회원</h3><p>haeon02로 로그인해 안내가 표시되지 않는지 확인합니다. 확인 기록은 안내를 읽었다는 의미입니다.</p></div></div><div class="say">“대응은 접근을 차단하고 안내를 남깁니다. 회원의 확인은 안내 수령 기록으로 처리됩니다.”</div></div></section>
<footer class="foot">시나리오 HAEON-DIAG-01 · 실행 자료 위치: <code>${esc(RUNS)}/&lt;실행 ID&gt;/</code> · 생성 ${esc(generated)}</footer>
</main></body></html>`;

fs.mkdirSync(path.dirname(OUT), {recursive:true});
fs.writeFileSync(OUT, html, 'utf8');
console.log(`HAEON-DIAG-01 촬영 큐시트 생성: ${OUT}`);
console.log(`Before: ${runName(beforeDir)} · After: ${runName(afterDir)}`);
